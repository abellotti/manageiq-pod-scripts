echo "Killing AuthConfig Pod ..."
kill-pods-authconfig
echo "Waiting for Pod removal ..."
while :
do
  if [ -z "$(oc get pods 2>/dev/null | egrep 'httpd-authconfig')" ]
  then
    break
  fi
  sleep 1
done
echo "Deploying Httpd AuthConfig ..."
deploy-authconfig -f
